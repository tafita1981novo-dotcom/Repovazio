# Indice - Acesso Mestre Claude (ponteiro publico, sem segredos)

> Os detalhes sensiveis (VM, credenciais, IPs, conexao) NAO ficam aqui (repo publico).
> Em QUALQUER chat novo do Claude, peca: "carregue meu acesso mestre".

O Claude deve buscar nesta ordem:
1. **Supabase** (projeto Cerebrodani = tpjvalzwkqwttvmszvie), tabela `claude_acesso_mestre`:
   `select conteudo from claude_acesso_mestre order by atualizado_em desc limit 1;`
   (Contem: como conectar na VM Google graphiti-memory por SSH autonomo, projeto/zona,
    cerebro auto-evolutivo, dashboard, afiliados, regras.)
2. **Arquivos locais** no laptop: `D:\CLAUDE\` e `D:\projetocode\`
   (estrategias, cerebro_schema.sql, dashboard_real.html, vm_google\, pacote_global\).

## Plataforma
13 canais YouTube de psicologia faceless (gaming/gambling pelo ponto de vista dos pais),
pipeline $0 (Pollinations FLUX + EdgeTTS), 10 idiomas, cerebro auto-evolutivo no Supabase.

## Regras absolutas
- ZERO metrica/conteudo simulado (so YouTube Studio>Ganhos / painel afiliado).
- Input editorial humano por video (Inauthentic Content Policy 2026).
- NUNCA apagar arquivos que o dono ja colocou.
